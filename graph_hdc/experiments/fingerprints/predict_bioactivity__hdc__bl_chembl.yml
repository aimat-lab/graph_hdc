pycomex: true
extend: predict_bioactivity__hdc.py
name: hdc_bioactivity_bl_chembl
parameters:
  # Dataset parameters
  DATASET_NAME: "bl_chembl_cls"
  DATASET_NAME_ID: "bl_chembl_cls"
  NUM_DATA: null  # Use full dataset (set to 1000 for quick testing)
  SEED: 1

  # Evaluation parameters
  NUM_QUERY_ACTIVES: 5        # Number of active queries per repetition
  NUM_REPETITIONS: 50          # Statistical repetitions per target
  MIN_ACTIVES_PER_TARGET: 10   # Minimum actives required per target

  # Metric parameters
  BEDROC_ALPHA: 20.0          # Standard α=20 for virtual screening
  EF_PERCENTAGES: [0.01, 0.05]  # EF at 1% and 5%

  # HDC encoding parameters
  EMBEDDING_SIZE: 2048         # Hypervector dimensionality
  NUM_LAYERS: 2                # Message passing steps
  BATCH_SIZE: 600
  DEVICE: "cpu"                # or "cuda:0" for GPU
  ENCODING_MODE: "continuous"  # or "categorical"

  # Visualization
  NUM_EXAMPLE_TARGETS: 3
  FIGURE_DPI: 300

  NOTE: "HDC-based bioactivity prediction on BL ChEMBL with 35 targets"
